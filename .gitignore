.env
__pycache__/
*.pyc
.venv/
venv/

# "brain" da memoria de projeto — local, nao versionado (ver docs/00-overview.md)
docs/

# eval harness output (regenerated on every run)
eval/last_report.json

# batch reports (generated)
reports/

# Streamlit secrets (real values must never be committed)
.streamlit/secrets.toml
.claude/
